[package]
name = "zkcredit-program"
version = "0.1.0"
edition = "2021"

[dependencies]
sp1-zkvm = "4.2.1"
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"] }

[dev-dependencies]
serde_json = "1.0"
